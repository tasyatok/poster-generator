function setup() {
  createCanvas(650, 910);
}

function draw() {
  background(0);
  fill(255);
  textSize(24);
  textAlign(CENTER, CENTER);
  text("Hello from your poster generator", width / 2, height / 2);
}